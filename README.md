# videoPlayer-react is a react project that uses react components' state to props to update the content dynamically. This is an example of how stateless components in react change stafeul components.
